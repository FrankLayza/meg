# Contributor 3: Demo Surface

This folder owns the independently runnable presentation surface. It renders shared fixtures without requiring the Guardian, Sibyl, or Base packages to be installed.

Later, replace fixture loading with adapters that read Contributor 1's decision JSON and Contributor 2's policy result JSON. Keep the visual distinction between model recommendation, deterministic policy, and transaction receipt.
